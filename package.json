{
  "name": "defiway-pay-landing",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "clean": "rm -rf dist",
    "deploy": "npm run build && gh-pages -d dist",
    "pages:build": "vite build --mode production",
    "pages:preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.0.8",
    "autoprefixer": "^10.4.16",
    "postcss": "^8.4.32",
    "cssnano": "^6.0.1"
  }
}
